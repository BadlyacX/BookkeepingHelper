# 記帳工具 — 技術架構規劃

## 需求背景
- 個人使用的記帳工具
- 沒有電腦可以長期開機當伺服器 → 選擇免維運的雲端服務
- 手機是 iOS,但手邊沒有 Mac → 排除需要 Xcode 的原生 iOS App 開發
- 不想繳 Apple Developer Program 年費($99 美金)→ 排除上架 App Store / Ad Hoc 安裝到實機的路線

## 最終方案:PWA(漸進式網頁應用)

### 什麼是 PWA
本質是一般網站,但透過以下技術讓它「用起來像 App」:
- `manifest.json`:定義 App 名稱、icon、啟動畫面、顏色
- Service Worker:快取靜態資源,支援一定程度的離線開啟
- iOS 上透過 Safari「加入主畫面」,產生桌面 icon,全螢幕開啟(無網址列)

### 為什麼選這個,而不是原生 App
| 考量 | PWA | 原生 App(React Native + EAS Build) |
|---|---|---|
| 開發環境 | 不需要 Mac | 不需要 Mac(EAS 雲端建置) |
| 費用 | 免費 | Apple Developer 年費 $99 |
| 離線能力 | 有限(需額外設計同步機制) | 完整(本機 SQLite) |
| 推播/背景執行 | iOS Safari 支援有限 | 完整 |
| 使用門檻 | Safari 分享 → 加入主畫面 | App Store 下載 或 Ad Hoc 安裝 |

## 技術棚(Tech Stack)

### 前端
- **框架**:Next.js(與 Vercel 部署整合度最高,PWA 設定也有現成套件)
- **語言**:TypeScript / JavaScript

### 部署平台
- **Vercel**(免費 Hobby 方案即可,不需自己維運伺服器)

### 資料庫(已定案:Supabase)
透過 **Vercel Marketplace** 安裝 Supabase 整合(底層是 Postgres)。

選 Supabase 而非 Neon 的原因:確定會多裝置使用、需要登入機制 → Supabase 內建 Auth 剛好解決這個需求,還附帶現成 REST API(PostgREST),前端可以直接呼叫,少寫一層 API。

### 前端本機儲存(離線用)
- **IndexedDB** — 離線時暫存待同步的記帳資料,恢復網路後同步上雲端

### PWA 相關設定檔
- `manifest.json` — App 名稱、icon、啟動畫面
- Service Worker(`sw.js`)— 快取靜態資源、支援離線開啟

### 一句話總結整個架構
用 Next.js + TypeScript 寫網站,資料存在 Supabase(Postgres),部署在 Vercel,加上 `manifest.json` + Service Worker 做成 PWA,iPhone 上用 Safari「加入主畫面」安裝,靠 Supabase Auth 的 session 持久性做到自動登入,沒網路時資料先寫 IndexedDB,恢復網路後同步上雲端。

### 資料表(初步)
一張 `transactions` 表即可起步,欄位大致包含:
- 日期
- 金額
- 類別
- 備註

## 開發到使用的完整流程
1. 本機(Windows/Linux 皆可)用 Next.js 開發網站,寫前端介面 + API Routes 存取資料庫
2. 資料庫連線資訊透過 `vercel install` 安裝 Marketplace 整合後自動注入環境變數,不曝露在前端
3. 部署到 Vercel(`git push` 即自動部署)
4. 設定好 `manifest.json` 與 Service Worker
5. 用 iPhone Safari 打開網站 → 分享選單 →「加入主畫面」
6. 桌面出現 icon,之後開啟即為全螢幕、App 化的使用體驗

## PWA 在 iOS 上的已知限制
- 離線只能保留「介面能打開」,實際存新資料仍需網路連到資料庫(除非額外做 IndexedDB 本機暫存 + 之後同步)
- Web Push 背景推播支援較陽春,不能依賴它做記帳提醒
- 「加入主畫面」需要使用者自己手動操作一次,沒有像 App Store 那樣的安裝入口